<%= form_with(model: oauth_session) do |form| %>
  <% if oauth_session.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(oauth_session.errors.count, "error") %> prohibited this oauth_session from being saved:</h2>

      <ul>
        <% oauth_session.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
